<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:text="@string/vote_already_event"
        android:id="@+id/vote_already_title"
        android:padding="10dp"
        android:textSize="@dimen/big_text_size"
        android:textColor="@color/blue"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@color/item_green"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/vote_already_recycler_view"
        app:layout_constraintTop_toBottomOf="@id/vote_already_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>
